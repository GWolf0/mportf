import React, { RefObject, useEffect, useRef } from 'react'

function Background(){

    useEffect(()=>{
    },[]);

    return (
        <div className='w-screen h-screen bg-lightest'>
        </div>
    )
}

export default Background;